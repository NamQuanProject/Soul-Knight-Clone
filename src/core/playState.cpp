#include "playState.h"

void PlayState::handleEvent(const sf::Event& event) {
    // Handle gameplay events
}

void PlayState::update() {
    // Update game logic (e.g., player movements)
}

void PlayState::render(sf::RenderWindow& window) {
    // Render gameplay elements
}
